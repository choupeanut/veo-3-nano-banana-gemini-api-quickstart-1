version: '3'

services:
  veo-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: veo-studio
    restart: always
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
